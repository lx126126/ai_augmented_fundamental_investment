# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
# 测试临时目录。用通配是为了覆盖 --basetemp 的各种变体名
# （只写死 .pytest_tmp/ 时，一次 `.pytest_tmp_fresh` 就会污染 git status）。
.pytest_tmp*/

# 个人练习脚本（不入库）；正式的 pytest 测试在 tests/ 目录，须入库
/test*.py
/*_test.py

# 本地数据缓存（不提交原始行情/财务数据）
data/raw/
data/market/
data/cache/
data/warehouse/
data/*_daily_kline.csv
*.parquet
*.sqlite
*.db
*.duckdb

# 年报 PDF（巨潮原始文件，可重新下载）；但保留数据校验覆盖记录 json
data/validation/*.pdf

# 每日刷新日志（launchd 输出 + .last_success 闸门文件，本地留痕即可）
data/logs/

# 报告归档：只跟踪「观察池」标的，按需生成的报告不入库（方案 B，2026-09-17 拍板）
#
# 背景：产品上线后「每查一只就多一个 reports/<期>/<code>.html」，仓库会随使用无限膨胀，
#       而按需生成的多是随手查询的标的，没有归档价值。
# 注意：gitignore 对**已跟踪**文件无效 —— 新增此规则后，历史上已入库的非观察池报告
#       需要显式 `git rm --cached <file>`（文件保留在磁盘上，服务照常可读）。
# 维护：新增/移除观察池标的时，同步增删下面一行 !reports/**/<code>.html
reports/**/*.html
# 跟踪池（watchlist/watchlist.json，2026-09-17 起「生成过报告即入池」，共 11 只）
!reports/**/000651.html
!reports/**/00700.html
!reports/**/09992.html
!reports/**/600519.html
!reports/**/601088.html
!reports/**/601328.html
!reports/**/300061.html
!reports/**/600036.html
!reports/**/600887.html
# 中国海油 A/H 两只（小红书发布包素材，长期保留）
!reports/**/600938.html
!reports/**/00883.html

# ⚠️ templates/valueline.html 故意保持跟踪：
#   build() 会把最近一次渲染结果写到那里，tests/test_report_style.py 断言了这个路径，
#   scripts/export.py 也把它当输入用于导 PNG/PDF。它是「最近一次构建的样子」，
#   由观察池标的的构建维护；按需查询把它覆盖成冷门标的后，提交前用
#   `git checkout -- templates/valueline.html` 还原即可。

# 导出产物
*.png
*.pdf

# 环境与密钥
.env
.env.local
*.token

# 系统
.DS_Store
Thumbs.db

# WorkBuddy 本地记忆（含隐私，严禁入库）
.workbuddy/

# 投研日记（内部操作层：买卖/仓位/成本/决策心理，严禁入库）
journal/

# 备份产物（journal + memory 的 tar.gz 快照，本地冗余，不入库）
backups/

# 假设台账（主观判断：假设/验证/打脸/归因，严禁入库）
# 保留 watchlist/watchlist.example.json（跟踪池配置，客观研究范围）
watchlist/reviews/

# 个人 Code Review 工作笔记（自用草稿，含未定稿的审查记录，不入库）
docs/code-review-guide.md

# 面试话术文档（含短板清单与雷点清单；简历附了 GitHub 链接，公开不利，不入库）
docs/interview-narrative.md

# 简历（含真实姓名与联系方式，仓库为 public，严禁入库）
# 注：*.pdf 已由上面的规则覆盖，但 .md / .html 版本需单独挡
docs/李潇简历-*
